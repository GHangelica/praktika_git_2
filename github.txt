GitHub — это сервис для хостинга Git-репозиториев, 
позволяющий разработчикам хранить код и совместно работать над проектами.

git push — отправляет локальные коммиты в удалённый репозиторий.
git pull — забирает изменения из удалённого репозитория и объединяет их с локальной веткой.
